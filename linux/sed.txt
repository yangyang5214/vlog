。
今天来看一下 linux  的 sed 命令。
。
。
sed 命令通常用来做文本处理，我经常用来做文本替换。
。
。
比较简单，下面直接看几个例子。
。
。
创建一个测试文件。
。
。
我们把 字符串 1 替换为 字符串 test。
。
。
可以看到，输出内容被替换了，但是测试文件内容没变。
。
。
下面加 -i 参数看下效果。
。
cat  一下 测试文件，可知 i 参数会直接修改文件内容。
。
下面看下行操作。
。
以参数 d 为例子。
。
删除 1 到 3 行的内容。
。
。
同理，测试一下 -i 参数。
。
。
和 d 参数类似的还有 a 是追加，c 取代。都是行操作。。
